{
  "name": "space-combat-game",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "description": "A browser-based space combat game written in TypeScript.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && npx --yes http-server . -p 8080 -c-1",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test \"build-test/test/**/*.test.js\"",
    "test:only": "tsc -p tsconfig.test.json && node --test \"build-test/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.6.0"
  }
}
